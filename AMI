AMIは、

インスタンスを作る元になるテンプレート

Tokiさんの知識で言うと、

Ubuntu Server ISOに近いもの

ただし、ISOそのものというより、

このOS・この初期設定でサーバを作るためのひな形

という感じ。

たとえばEC2を作るときに、

Ubuntuにする？
Amazon Linuxにする？
Windows Serverにする？

を選ぶ。

その選ぶ元がAMI。

AMI = サーバ作成用のOSテンプレート
