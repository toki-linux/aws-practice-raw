権限系問題を解く3つの基本原則
原則1：「誰が」「何に」アクセスするかを図で分離する
まず、リソースの所有者（アカウント）とアクセスする側（アカウント）を明確に分けて考えます。

「今回はどちら側の設定を聞かれているか」を最初に確認する癖をつけましょう

原則2：2種類のポリシーの役割を区別する
権限系の問題では、常に以下の2種類のポリシーが登場します。

ポリシーの種類	どこに付与	役割
アイデンティティベースポリシー	IAMユーザー/ロール	「このユーザーは何ができるか」を定義
リソースベースポリシー	S3バケット、SQSキュー、SNSトピックなど	「このリソースに誰がアクセスできるか」を定義
同一アカウント内の場合：基本的にアイデンティティベースポリシーだけで完結します（リソースベースポリシーは補助的）。

クロスアカウントの場合：リソースベースポリシー側で、他アカウントのPrincipalを明示的に許可する必要があります。ここが今回の設問のポイントでした。


原則3：「評価の仕組み」を覚える
AWSの権限評価は、同一アカウントかクロスアカウントかで多少ロジックが異なります。

```flowchart TD
    Start["リクエスト発生"]
    Deny["明示的なDenyが<br/>どこかにあるか？"]
    DenyEnd["アクセス拒否"]
    SameAccount["同一アカウント内の<br/>アクセスか？"]
    IAMCheck["IAMポリシーで<br/>Allowがあるか？"]
    CrossCheck["リソースポリシーで<br/>Allowがあるか？"]
    Allow["アクセス許可"]
    
    Start --> Deny
    Deny -->|"あり"| DenyEnd
    Deny -->|"なし"| SameAccount
    SameAccount -->|"Yes"| IAMCheck
    SameAccount -->|"No（クロスアカウント）"| CrossCheck
    IAMCheck -->|"Allow"| Allow
    CrossCheck -->|"Allow"| Allow
```
覚え方のコツ：

Deny最優先：どこかに明示的なDenyがあれば、他がAllowでも必ず拒否される
クロスアカウントはリソース側が鍵：リソースベースポリシーのPrincipalに相手アカウントが明記されていないと、そもそも入り口にすら立てない
問題文を読むときのチェックリスト
権限系の問題文を読む際、以下を順番に確認すると迷いが減ります。

同一アカウントか、クロスアカウントかを最初に判断する
「誰が」（Principal）の設定を聞かれているか、それとも**「リソース側」の設定を聞かれているか**を見極める
設問の主語（「〇〇部門はどうすべきか」）に注目し、その部門が所有しているリソース側の設定を聞かれていないか確認する
選択肢に出てくるサービス（IAMポリシー、リソースポリシー、SNS、STSなど）が、リソース側の仕組みか、Principal側の仕組みかを分類する
