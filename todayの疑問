ルートテーブルでは具体的な方を優先する。
0.0.0.0/0と10.0.0.0/16が設定されている場合
宛先IPが10.0.2.10は10.0.0.0/16に含まれるのでそちらに送られる。
これを 最長プレフィックス一致 と呼ぶ。

言葉は難しいけど、意味はこれ。

より細かく指定されているルートが優先される
0.0.0.0/0 は「どれにも当てはまらなかった通信の最終出口」みたいなもの。
VPC内通信は、より具体的な local ルートが優先される。
サブネットが使えるルートテーブルは1つ。
ただし、そのルートテーブルの中に複数のルートを書ける。
ルート伝播 = VPN Gatewayなどから受け取った経路情報を、ルートテーブルへ自動追加する仕組み。

今は、

手動でルートを書く = 今やっている基本
ルート伝播 = VPNやDirect Connectなど外部ネットワーク接続で使う応用

イメージはこれ。

サブネット = 1冊の地図だけ持てる
ルートテーブル = 地図
ルート = 地図の中に書かれた道案内

default セキュリティグループは、VPCごとに1つ自動で作られる。

だから今、

default セキュリティグループが2つある

なら、基本的には、

VPCが2つある
整理するとこう。

エラーの原因
→ IPv4 CIDR を書くタイプの既存ルールに、SG ID を入れようとしたから

Security Group の Source には、大きく2種類ある。

1. CIDR指定
   例：My IP、10.0.1.0/24、10.0.1.10/32

2. Security Group参照
   例：public-ec2-sg

この2つは別物。
CIDRで許可するルールと、SGで許可するルールは別枠。SG参照として作ればCIDRは不要。
プレフィックスリスト = CIDRの詰め合わせセット。

毎回CIDRを直接書く代わりに、

このリストに入っているIP範囲を許可する

または、

このリストに入っている宛先へはこのルートを使う

という形で使うもの。
ホームディレクトリにたくさんファイルやディレクトリがある。この中から.pemで終わるファイルの権限情報だけを見たい。どのようなコマンドを使っていたっけ？

これでOK。

ls -l ~/*.pem

意味はこう。

ls -l     権限などの詳細表示
~         ホームディレクトリ
*.pem     .pemで終わるファイル
